services:
  mongo:
    image: mongo:7
    container_name: cove-mongo
    ports:
      - "27017:27017"
    volumes:
      - cove_mongo_data:/data/db
    restart: unless-stopped

volumes:
  cove_mongo_data:
