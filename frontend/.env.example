# Local dev: leave empty — Vite proxies /api to localhost:5000
VITE_API_URL=

# Production (Vercel): DO NOT set VITE_API_URL.
# Use same-origin /api via vercel.json rewrites (see scripts/generate-vercel-config.mjs).
# Set on Vercel instead:
#   BACKEND_ORIGIN=https://cove-booking.onrender.com
